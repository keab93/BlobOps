services:
  agar:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
  bot:
    build: 
      context: .
      dockerfile: Dockerfile_bot
    depends_on:
      - agar
    # (Optional) Mount the config file for hot-reloading without reconnecting
    # volumes:
    #   - ./src/bot/config.json:/usr/src/app/src/bot/config.json
